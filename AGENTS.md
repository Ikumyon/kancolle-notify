# プロジェクト開発ルール

## 1. 言語とコミュニケーション
- 回答はすべて必ず日本語で回答し、アーティファクト（PlanやTask、Walkthrough）も同じく日本語で出力すること。
- 明確な指示がない限り勝手にコードを書かないこと。

## 2. ビルド・重い処理の実行禁止ルール
- **時間のかかるビルド処理の勝手な実行禁止**:
  - `pyinstaller` や `cargo build`、その他パッケージング・本番ビルドなど完了に時間がかかるコマンドは、**ユーザーから「ビルドしてください」「実行してください」等の明確な指示がない限り、AI側で勝手に実行しないこと**。
  - コード修正後の検証は構文チェック（`python -m py_compile` や `cargo check` などの即座に終わる軽量チェック）や手順の提示にとどめること。
- **先回り実行の制限**:
  - ユーザーから「ok」などの承認を得た場合でも、コードの編集のみを行い、重いビルド処理を勝手に巻き込んで実行しないこと。

## 3. ライブラリ導入時の確認ルール
1. **インストールの前の確認**:
   プロジェクトに新しいライブラリを追加・インストールする際は、インストールコマンド（`pip install` 等）を実行する前に、必ず現在の実行環境やグローバル環境にすでにインストールされているか（例: `pip show <パッケージ名>` や `python -c "import <パッケージ名>"` など）を事前に確認すること。
2. **確認結果に応じた対応**:
   すでにライブラリがインストールされていることが確認された場合は、無駄なインストールプロセスを実行せず、次の開発ステップへ進むこと。

## 4. プロセスの後始末（クリーンアップ）ルール
- 検証やテスト、コマンド実行などでバックグラウンドプロセス（デーモンやGUI等）を起動した場合は、**確認終了後に必ずプロセスを停止（クリーンアップ）し、プロセスを残留させたりファイルロックを残したりしないこと**。
