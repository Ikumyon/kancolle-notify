{
  "name": "kancolle-notify",
  "version": "0.9.2-beta",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --import ./tests/no-network.js --test tests/*.test.js",
    "bundle:worker": "npx esbuild server/worker.js --bundle --format=esm --target=es2022 --outfile=installer/bundled-worker.js"
  },
  "engines": {
    "node": ">=22.13"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^7.3.1"
  }
}