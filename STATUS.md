# 現在の状態（0.3.0）

2026-09-10：予定表示・疲労回復を実装し、中央Workerへ反映済み。
Worker version: `8c2cf034-7858-4549-b7aa-d882fd8fa107`

- ポップアップに遠征・入渠・建造、艦隊別の疲労回復見込み、手動予約を表示。
- 「あと何分／何時に」のセグメントコントロール。表示変更とカウントダウンは通信しない。
- 疲労回復の数字は設定で登録する。固定プリセット・固定既定値なし。登録した数字を艦隊別に選択。
- 疲労度・編成・入渠・補給・燃料・受信済みHTTP Dateから保守的に推定。野埼／野埼改の給糧は共通タイマーと発動条件を考慮し、給糧艦自身は除外。未計測・条件不足時は理由を表示して予約しない。
- 出撃・演習・給糧アイテム使用などの後は母港の実値で再計算。疲労回復も中央から「見込み」として通知。
- 送信内容・送信待ち・履歴の表示は既存の予約と履歴ページへ集約。
- UUID／世代方式、変更がなければ送らない方式を維持。連番はバックグラウンドで付け、目標変更とゲーム観測を一つの順序で送る。
- 疑似データで通常回復・給糧条件・編成例外・目標変更・通知と訂正を検証。既存の解析・再送・設定保存も確認。
- 実ブラウザでローカルファイルと模擬データだけを使い、ポップアップ・設定保存・セグメント切り替えを確認。構文確認とWrangler dry-run成功。
- 本番更新はWorkerコードのみ。既存予約の初期化・変換は行っていない。本番へのテスト通知は送っていない。
- 艦これサーバーへのリクエストは行っていない。

利用開始：通知拡張を再読み込みし、DevToolsも開き直す。設定の「疲労回復の目標値」で選択肢と既定値を登録する。
給糧タイマーを途中から取得して開始時刻が分からない場合は「未計測」。通常の編成変更などで開始を観測してから推定する。
自然回復・給糧の見込みは受信情報と条件継続を前提とする。詳細はREADMEの0.3.0節を参照。

## 0.2.0の実装記録

新方式を実装し、中央Workerへ反映済み。旧予約は変換せず破棄して初期化しました。

- 時計同期・5分ごとの定期通信を廃止。
- 艦これの終了予定Unix時刻を補正せず送信。
- プレイ開始時にUUID＋64ビット世代番号を取得。初回取得と変更時だけ送信。
- 同一世代の連番で重複・逆順を拒否し、別プレイ開始後の古い送信を拒否。
- 世代番号の上限でUUIDを変更し1へ戻す。バックグラウンド再送では新世代を取得しない。
- 同期応答の状態を表示に使い、画面更新の重複GETを削減。更新ボタンは明示的に取得。
- 本番/v2/status応答と旧予約0件を確認。
- 世代切替・上限・同内容10回で追加通信0回・再送復元・取消・模擬配信・旧API拒否・初期化をローカル検証済み。構文検証成功。
- 艦これサーバーへの追加リクエストは実行していません。

利用側ではDameconの通知拡張を再読み込みし、DevToolsも開き直して通常プレイの情報から取得を再開してください。
中央APIの新旧互換処理はありません。旧送信待ちは破棄し、新たに受信した情報を送信します。
本番の新方式での手動予約・通知はユーザー確認済み。

## 2026-09-11 泊地修理 0.4.0

実装済み: 受動観測のAkashiTracker、KC3準拠の推定HPと艦別全回復時刻、20分・艦別全回復の中央通知、艦隊内進捗一覧、クレーンSVG付きポップアップ。中央データは既存のJSON状態保存を使用し、通知済みマイルストーンを周期・艦ごとに保持する。

検証結果:
- `npm.cmd test`: 全36件成功。追加6件でKC3の実コードと計算境界を照合、20分と1隻全回復の集約、途中回復の通知抑止、後続艦の全回復、遅延・取消・再送、施設数・中破境界・入渠・0.85補正、編成・プリセット・随伴解除を確認。
- 受信データ解析→background→模擬Worker/D1→模擬配信の結合テスト成功。変更なし時の中央通信なし、クライアントからの追加通信なしで時間経過による配信を確認。
- `npm.cmd run check`: JavaScript構文・JSON成功。
- `node tools/browser-smoke.js`: Chrome 152の専用プロファイルで拡張読込・背景メッセージ・ポップアップ・設定画面を確認。クレーン、現在推定HP、全回復／修理中の混在表示を検証。ランタイムエラー0。画像は `.local/browser-smoke/popup_active.png`。
- ブラウザ検証は外部ホストの名前解決を無効化。艦これへのリクエスト、本番Workerへの公開、実Telegram送信は行っていない。

制約: 開始時刻を観測していない場合は未計測。戦闘・装備変更などで条件が不明になった場合は解除し、必要な通常応答を受信後、通常編成変更等で開始を確定する。UIの値は推定。実機のゲーム応答での確認は未実施。本番利用にはWorker更新と拡張再読み込みが必要。
