# 明石修理（泊地修理）対応計画

作成日: 2026-09-11。対象は既存の kancolle-notify。ローカル実装を完了。本番更新は行っていない。検証結果と実装上の未計測条件はREADME・STATUSを参照。

## 方針

遠征・入渠・建造・疲労回復に、泊地修理の表示と中央通知を追加する。ローカルの KC3 配布コードを計算と状態遷移の参考にし、KC3 の内部状態には依存しない。以下の数値は参照コードの仕様であり、現行ゲームの実測結果ではない。

通知対象は「20分経過による修理開始」と「各艦娘の全回復見込み時刻」の2種類。艦隊全員の全回復を待たず、1隻でも全回復する時点でその艦を通知する。途中のHP増加では通知しない。通知・画面にはKC3への言及を表示しない。計算はKC3を参考にし、推定値であることは「回復見込み」で表す。各行を `艦娘名: 数字->数字 +差分数字　あと数字分　HH:mm` とし、全回復までの残り分数と終了予定時刻を併記する。ゲームに追加通信・操作は行わない。

20分時点では修理開始とその時点の回復量を1通にまとめる。20分時点で全回復する艦は同じ通知内に全回復見込みとして明記し、別便で重複通知しない。以後は艦ごとの全回復見込み時刻を予約する。中央の毎分確認で同時に該当する艦を1通にまとめる。通知対象時刻は全回復時刻であり、配信は既存の毎分確認により通常0〜60秒程度後になる。

```text
第1艦隊 泊地修理開始（20分経過）
HP回復見込み
明石改: 35->37 +2　あと10分　14:30
吹雪改二: 28->30 +2　あと25分　14:45

母港で実際の回復を確認してください。
```

数値・時刻は14:20時点の書式例。残り時間は各艦の全回復見込み時刻から表示・通知作成時刻を引き、分単位で切り上げて0以上にする。終了予定時刻は日本時間のHH:mm、日付をまたぐ場合は日付も付ける。全回復見込み時刻に達した艦は残り分数と終了予定時刻を「全回復」に置き換え、`艦娘名: 開始HP->最大HP +差分　全回復` と表示する。「全回復」は推定上の全回復を表す。時間が未計測・計算不能なら「完了時刻未計測」とし、数字を作らない。通知と画面の両方で、修理中の艦は残り分数と終了予定時刻、全回復見込みの艦は「全回復」を表示する。全回復通知には同じ艦隊の修理対象艦一覧を載せ、修理中の艦の進捗も確認できるようにする。通知を発生させる条件は20分経過と各艦の全回復だけで変えない。

```text
HP回復見込み
明石改: 35->37 +2　全回復
吹雪改二: 28->30 +2　あと25分　14:45
```

差分は右辺−左辺に一致させるため、35->37の場合は+2とする。

左辺は当該修理周期の開始時に観測したHP、右辺は通知対象時刻の推定HP、差分はその周期内の累計回復量。追加通知も同じ基準を使い、前回通知からの差分と混同しない。修理対象外・周期開始時から全快の艦は回復通知に載せない。同じ艦隊の修理中の対象艦は回復増分が0でも進捗一覧に載せる。

## HP回復量の計算

`KC3AkashiRepair.getProgress`、`calculateRepairTime`、`calculateTickLength`、`calculateProgress` に合わせる。単位はミリ秒。

- 開始HPを H、最大HPを M、不足HPを D=M−H、経過時間を E とする。D=0なら回復0。開始時刻・修理時間・HPが不明なら推定しない。
- E<1,200,000（20分）なら回復0。
- 修理時間 T=ceil((api_ndock_time−30,000)×補正/60,000)×60,000。
- 1HPの間隔 I=ceil(T/D)。20分以降は、floor(E/60,000)×60,000 が I×n 以上になる最大の n（0〜D）を求める。n=0でも最低1HP回復とするKC3の分岐を適用する。
- 右辺は H+回復量（最大M）、差分は回復量。推定値は実測HPに上書きしない。
- 全回復通知時刻はKC3の回復量がDに達する最初の時刻とする。D=1なら最低1HP回復の分岐により開始から20分。D>1なら開始から max(1,200,000, ceil(I×D/60,000)×60,000) ms。途中の1HPごとの時刻は通知に使わない。全回復通知の右辺は最大HP、差分は最大HP−周期開始HP。
- 不正値・計算不能な状態では通知を保留する。母港で新しい実測HPと修理時間を受け取ったら、それを次周期の基準にする。実測差分を泊地修理による回復と無条件に断定しない。

## KC3から確認した根拠

参照: `../kc3kai-release/library/objects.js` の KC3AkashiRepair、KC3Fleet、KC3Ship、および `library/modules/Kcsapi.js` の編成ハンドラ。

| 項目 | KC3の実装 |
|---|---|
| 修理艦判定 | 艦種 api_stype=19、かつ明石型 api_ctype=49 または装備 api_type[2]=31 を所持 |
| 稼働条件 | 旗艦が上記の修理艦、中破未満、入渠・遠征中ではない |
| 修理可能隻数 | 旗艦と2番艦の稼働可能な修理艦について、修理施設数と明石型の基礎2隻を合計。先頭からその範囲を対象にする |
| 対象艦 | 範囲内、HPが最大HPの50%超、入渠・遠征中でない。全快艦は時間計算対象外 |
| 時間補正 | 旗艦・2番艦が hasRepairFlagship / hasRepair2ndShip の条件を満たすと0.85、それ以外1。KC3ではこの判定と稼働条件の判定が異なるため、勝手に統合しない |
| 基本所要時間 | ceil((api_ndock_time − 30,000) × 補正 / 60,000) × 60,000 ms。全快見込みは最低20分 |
| 回復表示 | 基本所要時間÷不足HPを切り上げて1HPの間隔を算出。20分未満は回復0、以後は分単位の丸めと最低1HP回復の分岐がある |
| タイマー | 全艦隊共通。通常編成変更で条件を満たす艦隊がある場合に再開始。艦隊間移動は両艦隊を処理 |
| プリセット | タイマー稼働中は維持、未開始なら条件成立時に開始。随伴一括解除も通常変更と同じリセット処理にしない |
| 母港 | 20分以上経過していれば、修理旗艦がある場合に共通タイマー再開始、なければ停止 |

## 実装構成

1. `extension/core/akashi.js` を追加し、AkashiTracker と純粋な見込み計算を実装する。修理タイマーは疲労回復の15分タイマーとは別に持つ。
2. `core/parser.js` で艦マスターの艦種・艦型、装備マスターの種別、所有装備ID→マスターID、艦の装備・HP・api_ndock_time・編成・入渠状態を受信情報から収集する。既存の疲労処理には修理時間・装備情報がないため追加が必要。
3. 母港、ship2/ship3/ship_deck、deck、ndock、slot_item、require_info、編成変更・プリセット・装備変更を扱う。装備の作成・廃棄・改修・艦解体なども、必要な状態を更新できなければ当該見込みを無効化する。API名・要求許可リスト・返却構造は実装時にKC3ハンドラと照合する。
4. 開始時刻は既存の疲労処理と同様に成功応答のHTTP Dateを使用する。KC3のDate.now()は採用しない。途中監視で開始を観測していない場合は「開始時刻未計測」とし、初回母港を開始と捏造しない。
5. 母港では受信したHP・修理時間で再計算してから次の周期を扱う。時刻経過だけで実測HPを書き換えない。出撃・演習・入渠・不明な装備変更などで前提が崩れた場合は、見込み予約を取り消し、通常通信で条件を再確認する。
6. `background.js` にスナップショット保持・差分送信を追加。`core/state.js` に泊地修理専用種別を追加し、入力検証、活動世代、通知済み判定、文面を拡張する。中央に周期ID・開始時刻・対象艦ID/艦名・開始HP・最大HP・修理時間・補正を保存し、共通の純粋関数で回復量と次の通知時刻を計算する。20分通知は周期ID、全回復通知は周期ID＋所有艦IDで識別し、それぞれ1回だけ通知する。既存の「活動につき1回」の通知済み判定をそのまま流用しない。通知遅延時は期限に達した未通知の全回復艦を1通にまとめる。
7. ポップアップに艦隊、対象艦、観測HP、推定HPと累計回復量、20分経過時刻、艦ごとの全回復見込み時刻、未計測・対象なし・情報待ちの理由を表示。泊地修理の各艦行では、修理中は既存の「あと何分／何時に」切替によらず「あと数字分」と終了予定時刻を両方表示し、全回復見込み時刻に達したら「全回復」に置き換える。残り時間の画面更新だけでは通信しない。

## ポップアップUI

- ポップアップに「泊地修理」セクションを追加し、見出しのアイコンはクレーンとする。既存アイコン資産で利用可能か確認し、なければクレーンを表すSVGを用意する。
- 艦隊ごとに20分経過までのタイマーと、修理対象の艦娘一覧を表示する。
- 各艦の行は `艦娘名: 開始HP->現在の推定HP +累計回復量　あと数字分　HH:mm`。全回復見込み時刻に達した艦は末尾を「全回復」に置き換える。
- 表示中は取得済みデータと経過時間から推定HP・差分・残り分数を更新する。右辺に将来の最大HPを先取りして表示しない。
- 幅が足りない場合は残り時間と予定時刻を次行に配置し、艦名・HP・時刻の欠落や横スクロールを避ける。既存ポップアップの色・文字サイズ・余白に合わせる。
- 未計測・情報待ち・対象なしも表示し、時刻やHPを捏造しない。クレーンアイコンに加えて「泊地修理」の文字を表示する。
- 実装中のUI確認も模擬データを使う。実装・検証・運用のいずれも艦これサーバーへの追加リクエスト、自動更新、ポーリング、ゲーム操作を行わない。
## 通知と不確実性

- 通知の見出しで「HP回復見込み」と明示し、艦別の行は指定書式を守る。20分通知は周期につき1回。以後は新たに全回復見込み時刻に達した未通知艦が1隻でもあれば通知する。本文は同じ艦隊の対象艦一覧とし、全回復見込みの艦を「全回復」、修理中の艦を残り分数・終了予定時刻で表示する。一覧への再掲だけでは通知済み艦の全回復通知を再発生させない。
- PC終了後も中央で保存した見込み時刻を通知する。条件が維持される前提であり、中央がゲームを確認することはない。
- 20分到達後の追加回復は同じ開始時刻を基準に計算する。自動で20分周期を再開始しない。母港の観測で更新した周期だけを新しい活動として扱う。
- 共通タイマーの再開始で全艦隊の見込みを再計算する。変更のない観測は送信しない。古い周期・旧端末の再送で取消済み予約を復活させない。
- 欠落応答や失敗応答を空き状態と解釈しない。不明状態と、条件不成立が確定した状態を分ける。
- 新しい正式方式として実装し、旧形式の互換レイヤや二重実装は設けない。

## 検証・完了条件

1. 明石・明石改、施設数、2番艦の修理艦、0.85補正、修理範囲外、全快、中破ちょうど、入渠・遠征、複数艦隊を単体テストする。
2. 19分59秒／20分、分単位切り上げ、1HP不足、20分で複数HP回復、最低1HP分岐と2HP目、最大HP上限、長時間修理、開始未観測、HTTP Date欠落を確認する。KC3のcalculateProgressと同じ入力で回復量・艦ごとの全回復時刻を照合する。
3. 通常編成変更、艦隊間移動、プリセット、随伴一括解除、施設付け替え、母港再計算、途中監視、セッション更新を模擬する。
4. 指定された艦名・HP・累計差分・残り分数・終了予定時刻の書式、残り分数の切り上げ・日付またぎ・未計測表示、全回復時の「全回復」への置換、全回復した艦と修理中の艦の混在表示、20分時点の通知集約、途中回復で通知しないこと、1隻だけ先に全回復した場合の即時対象化、別時刻の各艦の全回復通知、20分と全回復の重複防止、全快後停止、遅延時の集約、二重受信、通知済み艦、取消、通信失敗再送、PC切替、ブラウザ終了後の中央通知を検証する。
5. `tools/mock.js` / `mock.html` に再現シナリオを追加し、ゲームへの追加通信ゼロ、変更なし時の中央通信ゼロを確認する。
6. `npm.cmd test` と `npm.cmd run check` を実行し、READMEとSTATUSを更新する。本番公開は実装後の別工程とする。
